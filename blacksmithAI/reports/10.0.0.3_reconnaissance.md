# Reconnaissance Report: 10.0.0.3

**Date:** 2026-07-01  
**Target:** 10.0.0.3  
**Report Type:** Initial Reconnaissance

---

## Executive Summary

This report documents the initial reconnaissance activities performed on target IP address 10.0.0.3. The investigation included DNS enumeration, port scanning, and OSINT gathering using various tools including nmap, dig, dnsrecon, and theHarvester.

---

## 1. Network Discovery

### 1.1 Port Scan Results (nmap)

**Command:** `nmap -sS 10.0.0.3`

**Findings:**
- **Host Status:** UP (0.00016s latency)
- **Open Ports:**
  - **Port 22/tcp** - SSH
  - **Port 80/tcp** - HTTP

**Analysis:** The target has two open services: SSH on port 22 and HTTP on port 80. This is a minimal attack surface, which is generally good from a security perspective.

---

## 2. DNS Enumeration

### 2.1 DNS Record Queries (dig)

**Commands Executed:**
- `dig 10.0.0.3 ANY` - Returns NXDOMAIN (no domain associated)
- `dig 10.0.0.3 NS` - No records found
- `dig 10.0.0.3 A` - No records found
- `dig 10.0.0.3 MX` - No records found
- `dig 10.0.0.3 TXT` - No records found
- `dig 10.0.0.3 SOA` - No records found
- `dig 10.0.0.3 PTR` - No records found
- `dig 10.0.0.3 CNAME` - No records found
- `dig 10.0.0.3 AAAA` - No records found
- `dig 10.0.0.3 SRV` - No records found

**Findings:**
- **Reverse DNS Lookup:** NXDOMAIN - No hostname associated with this IP
- **DNS Zone:** No active DNS zone found for this IP address
- **DNS Type:** This appears to be a standalone IP without domain association

### 2.2 DNS Enumeration (dnsrecon)

**Command:** `dnsrecon -t axfr -d 10.0.0.3`

**Findings:**
- No zone transfer available
- No NS servers found
- No SOA records found
- No CNAME records found

**Analysis:** The IP address 10.0.0.3 does not have an associated DNS zone, which suggests it may be:
- A private/internal IP address
- A bastion/hosting server
- A recently provisioned server without DNS configuration

---

## 3. OSINT Gathering

### 3.1 theHarvester Results

**Commands Executed:**
- `theHarvester -d 10.0.0.3 -b shodan` - No results (missing API key)
- `theHarvester -d 10.0.0.3` - No results found

**Findings:**
- No publicly indexed information found in Shodan
- No emails, hosts, or people associated with this IP in public databases

**Analysis:** The target appears to have a low public footprint, which is typical for internal or private infrastructure.

---

## 4. Web Service Analysis

### 4.1 HTTP Service Discovery (curl)

**Command:** `curl -s -I http://10.0.0.3/`

**Findings:**
- **Server:** uvicorn (Python web framework)
- **Proxy:** Squid/6.13 (reverse proxy detected)
- **Response Codes:** 200 OK (initial), 503 Service Unavailable (subsequent requests)
- **Cache Status:** z-bastion (indicates bastion/proxy caching)

### 4.2 Web Application Identification (httpie)

**Command:** `http GET http://10.0.0.3/`

**Findings:**
- **Application:** LiteLLM API
- **UI:** Swagger UI
- **OpenAPI Spec:** Available at `/openapi.json`

**Analysis:**
- **LiteLLM** is an API gateway that provides a unified interface to various LLM APIs
- The presence of Swagger UI indicates the service is well-documented
- The "z-bastion" cache status suggests this is a bastion/proxy server
- The 503 errors on subsequent requests may indicate rate limiting or connection issues

---

## 5. Summary of Findings

| Category | Finding | Risk Level |
|----------|---------|------------|
| Open Ports | 22 (SSH), 80 (HTTP) | Low |
| DNS Records | None found | N/A |
| Web Application | LiteLLM API with Swagger UI | Medium |
| Reverse Proxy | Squid 6.13 | Low |
| Web Framework | uvicorn (Python) | Low |
| Public Footprint | Minimal | Low |

---

## 6. Recommendations

Based on the reconnaissance findings:

1. **Further Investigation:** The LiteLLM API may provide access to various LLM services. Further investigation of the API endpoints is recommended.

2. **SSH Assessment:** Port 22 is open. Consider testing for SSH vulnerabilities and default credentials.

3. **Proxy Analysis:** The Squid proxy may be configured with specific rules. Further analysis of proxy configuration could reveal additional information.

4. **Internal Infrastructure:** This appears to be internal infrastructure (no public DNS). Consider if this IP should be accessible from external networks.

---

## 7. Tools Used

- **nmap** - Port scanning and service detection
- **dig** - DNS record queries
- **dnsrecon** - DNS enumeration
- **theHarvester** - OSINT gathering
- **curl** - HTTP service testing
- **httpie** - HTTP service testing

---

## 8. Conclusion

The target 10.0.0.3 is a minimal-service host with SSH and HTTP services running. The HTTP service hosts a LiteLLM API gateway behind a Squid proxy. The lack of DNS records and minimal public footprint suggests this is internal infrastructure. Further investigation should focus on the LiteLLM API capabilities and potential access to connected LLM services.

---

*Report generated on: 2026-07-01*
